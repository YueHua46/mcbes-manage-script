---
description: Minecraft 基岩 UI 表单（ActionForm 等）按钮文案配色约定
globs: scripts/ui/**/*.ts
---

# 按钮文案配色

- **不要使用 `§7`（灰色）或 `§8`（深灰）作为 ActionForm / ModalForm 按钮主文案或副标题**：在客户端上会显得整块按钮发灰、难读。
- 副标题、说明行优先使用 **`§b`（青色）**、**`§3`（深青）**、**`§e`（金色数字）**、**`§a`（绿色）**、**`§f`（白色）** 等与背景对比足够的颜色。
- 标题行可继续用 **`§w`**（白色加粗）突出主操作名。
