# 升级与数据备份

1. 停止服务器或退出世界。
2. 备份完整世界目录、行为包、资源包和服务器配置。
3. 确认新版本支持当前 Minecraft 版本。
4. 使用同一构建变体升级，除非已阅读迁移说明。
5. 替换行为包与资源包，并确认 manifest 版本已更新。
6. 完整重启，不要仅使用 `/reload`。
7. 验证玩家数据、经济、领地、公会、路点与假人是否正常。

资源包仍显示旧内容时，优先检查服务器实际部署目录、包 UUID、manifest 版本以及客户端缓存。不要在没有备份的生产世界中直接测试 Preview/Beta API 新版本。