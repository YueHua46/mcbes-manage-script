# BUG 检测队列

更新时间：2026-06-18

## 项目主链路

- 入口：`scripts/main.ts` 默认导入 `main.standard.ts`，BDS 增强版为 `main.bds.ts`。
- 启动加载：数据库、系统设置、试玩模式、一键功能、玩家名显示、自定义命令、黑名单、行为日志工具、公会模块。
- 事件注册：`eventRegistry.initializeAll()` 从 `scripts/events/handlers/index.standard.ts` / `index.bds.ts` 注册聊天、玩家、领地、PVP、行为日志、在线时间、领地飞行、统计、物品监控、反复制等事件。
- 用户入口：苦力怕菜单 `scripts/ui/forms/server/index.ts` 分发到经济、领地、公会、PVP、数据统计、系统、行为日志等 UI。

## 优先级定义

- P0：可越权、刷钱/吞钱、破坏他人领地、复制/丢失贵重物品、远程崩服。
- P1：绕过限制、错误计费、数据错乱、管理员误操作导致严重后果。
- P2：误导提示、冷却/限额不一致、性能退化、日志噪声。
- P3：体验瑕疵、代码健壮性与可维护性。

## 检测队列

| 状态 | 优先级 | 模块 | 检测点 | 方法 | 优化方向 |
| --- | --- | --- | --- | --- | --- |
| 已修复 | P0 | 领地 | 打火石/桶/容器/活板门/投掷器等交互绕过 | 检查 `playerInteractWithBlock` 分类顺序与关键字覆盖 | 危险物品优先拦截，容器/门类关键字兜底 |
| 已修复 | P0 | 领地 | 一键砍树/挖矿跨边界破坏领地 | 从领地边界外触发连锁，观察受保护方块是否被批量改成 air | 连锁任务逐格调用领地破坏权限 |
| 已修复 | P0 | 领地 | 超大范围圈地可通过重叠提示枚举他人领地坐标 | 提交覆盖大范围坐标并观察重叠错误回显 | 先按方块数上限拦截，重叠提示不暴露领主、领地名和坐标 |
| 已修复/需实测 | P0 | 领地 | 桶装流体、末地水晶、船、矿车、盔甲架等可能不触发 `playerPlaceBlock` | 对照 `itemStartUseOn` 与实际生成物 | 无权限使用后下一 tick 清理目标点附近敏感实体/流体，保留 API 无 before-cancel 的说明 |
| 已修复 | P0 | 经济/拍卖行 | 背包容量不足时购买扣钱但物品无法完整接收 | 购买超过剩余堆叠/空格容量的数量 | 购买前容量预检，发放时检查 `addItem` 剩余 |
| 已修复 | P0 | 经济/拍卖行 | 付款后发物品或更新库存失败可能吞物/错账 | 检查 `buyItem` 中付款、发货、ItemDatabase 更新顺序 | 发货前快照背包，成功发货后再改库存，失败恢复背包并退款 |
| 已修复 | P0 | 经济/官方商店 | 付款后发货或扣库存失败可能导致库存错乱/退款后仍拿到物品 | 检查官方商店 `executePurchase` 的扣费、发货、库存更新顺序 | 发货前快照背包，完整发货后再扣库存，失败恢复背包并退款 |
| 已修复 | P0 | 经济/红包 | 红包领取先到账后保存记录，写库失败可能重复领 | 检查 `claim()` 中 `addGold` 与 `db.set` 顺序 | 先保存领取记录，再发金币；发放失败回滚领取记录 |
| 已修复 | P0 | 经济/物品存储 | ItemDatabase 删除中间空实体导致槽位映射错位 | 检查 `#deleteItem` 中实体数组 `splice` 与 `slot` 计算 | 只回收末尾空实体，保持既有槽位号稳定 |
| 已修复 | P0 | 经济/PVP | PVP 击杀在经济关闭或到账失败时仍记录夺金 | 检查 `pvp-stats.handleKill` 中扣款、到账、统计顺序 | 仅经济开启且扣款/到账成功才累计夺金，到账失败尝试回滚 |
| 已修复 | P0 | 经济 | 玩家转账收款方写入失败可能扣款方少钱 | 检查 `transfer` 先扣后加的异常路径 | 收款写入失败时回滚付款方钱包 |
| 已修复 | P0 | 经济/出售 | 出售确认后换槽位或金币发放失败可能导致错价/吞物 | 检查单件出售与一键出售扣物、计价、到账顺序 | 执行时重新计价和校验数量，到账失败恢复物品 |
| 已修复 | P1 | 公会 | 创建费扣除后公会/索引保存失败会少钱 | 检查 `createGuild` 扣费与持久化顺序 | 保存失败回滚创建费 |
| 已修复 | P1 | 公会 | 每日红包先到账后记录领取日，保存失败可能重复领 | 检查 `claimDailyRedPacket` 顺序 | 先扣金库并保存领取日，再发钱；失败回滚 |
| 已修复 | P1 | 公会 | 金库取出/每日红包部分到账时公共金库仍按全额扣除 | 检查 `treasuryWithdraw` 与 `claimDailyRedPacket` 的到账判断 | 要求全额到账，部分到账撤回金币并恢复金库 |
| 已修复 | P1 | 管理入口 | 黑名单/官方商店/经济管理子入口依赖调用方校验 | 直接审查导出管理表单入口 | 导出管理入口增加自校验 |
| 已修复 | P1 | 经济 | 每日金币上限设置运行时不生效 | 修改 `daily_gold_limit` 后立即卖物/杀怪 | 每次读取限额时同步设置 |
| 已修复 | P1 | 经济/PVP | PVP 负数/异常配置导致保护、冷却、战斗标签失真 | 通过 `/yuehua:setting` 写入负数/NaN 后检查 PVP 规则 | PVP service 层统一归一化非负整数和最小时长 |
| 已修复 | P1 | 领地 | 创建个人领地扣费后保存失败会少钱 | 检查 `createLand` 扣费与 `db.set` 异常路径 | 保存失败时退回创建费用 |
| 已修复 | P1 | 领地飞行 | 管理从旁观/创造切生存后仍可飞 | 切换游戏模式后检查 mayfly/session | 切生存/冒险时清理脚本飞行 |
| 已修复 | P1 | 领地飞行 | 管理员领地飞行被扣金币 | 管理员开启飞行等待扣费周期 | 管理员跳过计费并清空下次扣费时间 |
| 已修复 | P2 | 随机传送 | 随机传送可能落到危险点或已有领地内 | 检查落点方块、脚下/头顶空间与领地检测 | 搜索安全表面并避开已有领地，失败时提示 |
| 已修复 | P2 | 传送点 | 领地/坐标点可能传送到窒息、岩浆、流体等危险位置 | 检查保存点/传送前的脚下和头顶方块 | 传送前统一校验安全落点，不安全则取消并提示 |
| 已修复 | P2 | 死亡回城 | 死亡地点数据异常或危险落点会导致报错/传送到危险位置 | 检查 `lastDeath` 解析和直接传送逻辑 | 捕获异常并复用安全落点校验 |
| 已修复 | P2 | 经济 | 杀怪奖励提示显示原金额而非实际到账 | 接近每日上限时杀怪 | actionbar 使用 `actualEarned` |
| 进行中 | P0 | 命令/UI | 注册为 Any 的命令是否有管理动作漏校验 | 枚举自定义命令与 UI 管理入口，逐个检查 `isAdmin`/权限级别 | 管理动作入口和 service 双层校验 |
| 待测 | P0 | 领地 | 更多实体放置物是否能绕过保护（画、物品展示框等） | 实测所有能右键生成实体的物品 | 纳入敏感实体清理白名单 |
| 进行中 | P0 | 经济 | 红包、公会金库、PVP 夺取、转账是否可绕日上限/刷钱 | 检查所有 `addGold(..., true)` 和转账回滚 | 明确白名单原因，失败路径事务化 |
| 进行中 | P0 | 物品存储 | ItemDatabase 拍卖/官方商店实体存储是否可丢失/重复取出 | 检查 store/unStore、实体加载、重复点击、无效 entry | 加锁/二次校验/失败回滚 |
| 待测 | P0 | 反复制 | 收纳袋、潜影盒、容器移动和管理员查看背包是否引入复制/吞物 | 检查 `addItem` 溢出、容器快照、UI 并发点击 | 所有移动操作处理 leftover，敏感操作加锁 |
| 待测 | P1 | 领地 | 爆炸、凋零、TNT、火焰蔓延是否能间接破坏保护区 | 检查 explosion、wither eject、fireTick 来源追踪 | 对实体源和无源持续伤害增加兜底 |
| 待测 | P1 | PVP | 强制/关闭/领地内 PVP 规则是否一致 | 检查 `pvp-manager` 与 `events/handlers/pvp.ts` | 统一判定函数，避免双处理 |
| 待测 | P1 | 试玩/黑名单 | 试玩、会员、封禁是否可通过改名/离线记录绕过 | 检查 XUID/玩家名映射、join 前/后处理 | 优先使用稳定 ID，名字仅展示 |
| 待测 | P1 | 公会 | 公会领地信任、金库提款、管理员操作是否有越权 | 检查 guild service 所有 owner/officer/admin 分支 | 服务层统一角色校验 |
| 进行中 | P1 | 数据库 | 大数据分块、JSON 解析、动态属性上限导致数据损坏 | 压测大钱包/日志/商店/领地数据 | 写入前校验大小，失败保留旧值 |
| 已修复 | P1 | 数据库 | JSON 损坏且自动修复失败时会覆盖原始损坏数据 | 检查 `Database.getAll` 解析失败后的默认值重置路径 | 重置前按时间戳分块备份原始损坏字符串 |
| 待测 | P2 | 行为日志 | 日志采集是否造成卡顿/刷屏/误报 | 检查高频事件、最大条数、快照频率 | 节流、采样、批量写入 |
| 进行中 | P2 | 随机传送/传送点 | 传送到危险位置或他人领地 | 检查维度、落点、私有点权限 | 安全落点搜索，私有点严格鉴权 |
| 进行中 | P2 | 设置系统 | 类型转换错误导致配置失效 | 检查布尔/数字/string 写入路径 | schema 化设置校验 |
